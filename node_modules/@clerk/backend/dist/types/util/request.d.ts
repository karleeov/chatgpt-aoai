/**
 * This function is only used in the case where:
 * - DevOrStaging key is present
 * - The request carries referrer information
 * (This case most of the times signifies redirect from Clerk Auth pages)
 *
 */
export declare function checkCrossOrigin({ originURL, host, forwardedHost, forwardedProto, }: {
    originURL: URL;
    host?: string | null;
    forwardedHost?: string | null;
    forwardedProto?: string | null;
}): boolean | "";
export declare function convertHostHeaderValueToURL(host?: string, protocol?: string): URL;
type ErrorFields = {
    message: string;
    long_message: string;
    code: string;
};
export declare const getErrorObjectByCode: (errors: ErrorFields[], code: string) => ErrorFields | null | undefined;
export {};
//# sourceMappingURL=request.d.ts.map