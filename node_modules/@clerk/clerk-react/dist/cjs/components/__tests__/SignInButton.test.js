"use strict";
var __create = Object.create;
var __defProp = Object.defineProperty;
var __getOwnPropDesc = Object.getOwnPropertyDescriptor;
var __getOwnPropNames = Object.getOwnPropertyNames;
var __getProtoOf = Object.getPrototypeOf;
var __hasOwnProp = Object.prototype.hasOwnProperty;
var __copyProps = (to, from, except, desc) => {
  if (from && typeof from === "object" || typeof from === "function") {
    for (let key of __getOwnPropNames(from))
      if (!__hasOwnProp.call(to, key) && key !== except)
        __defProp(to, key, { get: () => from[key], enumerable: !(desc = __getOwnPropDesc(from, key)) || desc.enumerable });
  }
  return to;
};
var __toESM = (mod, isNodeMode, target) => (target = mod != null ? __create(__getProtoOf(mod)) : {}, __copyProps(
  // If the importer is in node compatibility mode or this is not an ESM
  // file that has been converted to a CommonJS file using a Babel-
  // compatible transform (i.e. "__esModule" has not been set), then set
  // "default" to the CommonJS "module.exports" for node compatibility.
  isNodeMode || !mod || !mod.__esModule ? __defProp(target, "default", { value: mod, enumerable: true }) : target,
  mod
));
var import_testUtils = require("@clerk/shared/testUtils");
var import_react = __toESM(require("react"));
var import_SignInButton = require("../SignInButton");
const mockRedirectToSignIn = jest.fn();
const originalError = console.error;
const mockClerk = {
  redirectToSignIn: mockRedirectToSignIn
};
jest.mock("../withClerk", () => {
  return {
    withClerk: (Component) => (props) => {
      return /* @__PURE__ */ import_react.default.createElement(
        Component,
        {
          ...props,
          clerk: mockClerk
        }
      );
    }
  };
});
const url = "https://www.clerk.com";
describe("<SignInButton/>", () => {
  beforeAll(() => {
    console.error = jest.fn();
  });
  afterAll(() => {
    console.error = originalError;
  });
  beforeEach(() => {
    mockRedirectToSignIn.mockReset();
  });
  it("calls clerk.redirectToSignIn when clicked", async () => {
    (0, import_testUtils.render)(/* @__PURE__ */ import_react.default.createElement(import_SignInButton.SignInButton, null));
    const btn = import_testUtils.screen.getByText("Sign in");
    import_testUtils.userEvent.click(btn);
    await (0, import_testUtils.waitFor)(() => {
      expect(mockRedirectToSignIn).toHaveBeenCalled();
    });
  });
  it("handles redirectUrl prop", async () => {
    (0, import_testUtils.render)(/* @__PURE__ */ import_react.default.createElement(import_SignInButton.SignInButton, { redirectUrl: url }));
    const btn = import_testUtils.screen.getByText("Sign in");
    import_testUtils.userEvent.click(btn);
    await (0, import_testUtils.waitFor)(() => {
      expect(mockRedirectToSignIn).toHaveBeenCalledWith({ redirectUrl: url });
    });
  });
  it("handles afterSignInUrl prop", async () => {
    (0, import_testUtils.render)(/* @__PURE__ */ import_react.default.createElement(import_SignInButton.SignInButton, { afterSignInUrl: url }));
    const btn = import_testUtils.screen.getByText("Sign in");
    import_testUtils.userEvent.click(btn);
    await (0, import_testUtils.waitFor)(() => {
      expect(mockRedirectToSignIn).toHaveBeenCalledWith({
        afterSignInUrl: url
      });
    });
  });
  it("handles afterSignUpUrl prop", async () => {
    (0, import_testUtils.render)(/* @__PURE__ */ import_react.default.createElement(import_SignInButton.SignInButton, { afterSignUpUrl: url }));
    const btn = import_testUtils.screen.getByText("Sign in");
    import_testUtils.userEvent.click(btn);
    await (0, import_testUtils.waitFor)(() => {
      expect(mockRedirectToSignIn).toHaveBeenCalledWith({
        afterSignUpUrl: url
      });
    });
  });
  it("renders passed button and calls both click handlers", async () => {
    const handler = jest.fn();
    (0, import_testUtils.render)(
      /* @__PURE__ */ import_react.default.createElement(import_SignInButton.SignInButton, null, /* @__PURE__ */ import_react.default.createElement("button", { onClick: handler }, "custom button"))
    );
    const btn = import_testUtils.screen.getByText("custom button");
    import_testUtils.userEvent.click(btn);
    await (0, import_testUtils.waitFor)(() => {
      expect(handler).toHaveBeenCalled();
      expect(mockRedirectToSignIn).toHaveBeenCalled();
    });
  });
  it("uses text passed as children", async () => {
    (0, import_testUtils.render)(/* @__PURE__ */ import_react.default.createElement(import_SignInButton.SignInButton, null, "text"));
    import_testUtils.screen.getByText("text");
  });
  it("throws if multiple children provided", async () => {
    expect(() => {
      (0, import_testUtils.render)(
        /* @__PURE__ */ import_react.default.createElement(import_SignInButton.SignInButton, null, /* @__PURE__ */ import_react.default.createElement("button", null, "1"), /* @__PURE__ */ import_react.default.createElement("button", null, "2"))
      );
    }).toThrow();
  });
});
//# sourceMappingURL=SignInButton.test.js.map