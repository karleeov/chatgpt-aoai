"use strict";
var import_array = require("./array");
describe("toSentence", () => {
  it("returns a single item as-is", () => {
    expect((0, import_array.toSentence)(["xyz"])).toBe("xyz");
    expect((0, import_array.toSentence)(["abc"])).toBe("abc");
  });
  it('joins multiple items but the last with a comma and the last with ", or"', () => {
    expect((0, import_array.toSentence)(["abc", "def"])).toBe("abc, or def");
    expect((0, import_array.toSentence)(["qwe", "zxc", "asd"])).toBe("qwe, zxc, or asd");
  });
  it("returns empty string if passed an empty array", () => {
    expect((0, import_array.toSentence)([])).toBe("");
  });
});
//# sourceMappingURL=array.test.js.map