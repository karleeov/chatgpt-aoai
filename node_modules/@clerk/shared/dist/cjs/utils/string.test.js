"use strict";
var import_string = require("./string");
describe("isIPV4Address(str)", () => {
  it("checks if as string is an IP V4", () => {
    expect((0, import_string.isIPV4Address)(null)).toBe(false);
    expect((0, import_string.isIPV4Address)(void 0)).toBe(false);
    expect((0, import_string.isIPV4Address)("")).toBe(false);
    expect((0, import_string.isIPV4Address)("127.0.0.1")).toBe(true);
  });
});
describe("titleize(str)", () => {
  it("titleizes the string", () => {
    expect((0, import_string.titleize)(null)).toBe("");
    expect((0, import_string.titleize)(void 0)).toBe("");
    expect((0, import_string.titleize)("")).toBe("");
    expect((0, import_string.titleize)("foo")).toBe("Foo");
    expect((0, import_string.titleize)("foo bar")).toBe("Foo bar");
  });
});
//# sourceMappingURL=string.test.js.map