import { OpenAIClient } from "../../../src/index.js";
import { Recorder } from "@azure-tools/test-recorder";
import { Test } from "mocha";
import { ClientOptions } from "@azure-rest/core-client";
export type AuthMethod = "AzureAPIKey" | "OpenAIKey" | "AAD" | "DummyAPIKey";
export declare function createClient(authMethod: AuthMethod, options: {
    recorder?: Recorder;
    clientOptions?: ClientOptions;
}): OpenAIClient;
/**
 * starts the recorder and reads the environment variables from the `.env` file.
 * Should be called first in the test suite to make sure environment variables are
 * read before they are being used.
 */
export declare function startRecorder(currentTest?: Test): Promise<Recorder>;
//# sourceMappingURL=recordedClient.d.ts.map